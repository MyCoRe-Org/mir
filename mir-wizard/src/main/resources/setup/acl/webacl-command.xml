<?xml version="1.0" encoding="utf-8"?>
<commands>
  <command file="resource:setup/acl/grant-editors.xml" describeBy="administrators and editors">
    update permission read for id
    webpage:/search/search-expert.xed with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-guest-and-submitter.xml" describeBy="guests and submitters">update permission read for id
    webpage:/content/main/search/complex.xed with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-editors.xml" describeBy="administrators and editors">update permission read for id
    webpage:/content/main/search/complex_intern.xml with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/authorization/new-user.xml?action=save with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-submitter.xml" describeBy="submitters, editors and administrators">update permission read for id
    webpage:/content/publish/index.xml with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-all.xml" describeBy="not logged in">update permission read for id
    webpage:/authorization/new-author.xed with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/authorization/roles-editor.xml with
    rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/servlets/MCRSessionListingServlet with
    rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/servlets/MCRLockServlet?url=/editor_form_commit-mods-all.xml with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/servlets/MCRUserServlet with rulefile
    {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/modules/classeditor/classificationEditor.xml with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/rsc/ACLE/start with rulefile {file}
    described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission read for id
    webpage:/modules/wcms2/wcms2.xml?XSL.useMT=false with rulefile {file} described by {describeBy}
  </command>
  <command file="resource:setup/acl/grant-admin.xml" describeBy="administrators only">update permission write for id
    webpage:/modules/wcms2/wcms2.xml?XSL.useMT=false with rulefile {file} described by {describeBy}
  </command>
</commands>