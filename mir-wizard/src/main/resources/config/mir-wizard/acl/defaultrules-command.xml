<?xml version="1.0" encoding="utf-8"?>
<commands>
  <command file="resource:config/mir-wizard/acl/permissions.xml">load permissions data from file {file}</command>
  <command file="resource:config/mir-wizard/acl/grant-all.xml" describedBy="always allowed">update permission read for id default with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-admin.xml" escribedBy="administrators only">update permission writedb for id default with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-admin.xml" escribedBy="administrators only">update permission deletedb for id default with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-all.xml" escribedBy="always allowed">update permission view for id default_mods with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-all.xml" escribedBy="always allowed">update permission read for id default_mods with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-editors.xml" escribedBy="administrators and editors">update permission writedb for id default_mods with rulefile {file} described by {describedBy}</command>
  <command file="resource:config/mir-wizard/acl/grant-admin.xml" escribedBy="administrators only">update permission deletedb for id default_mods with rulefile {file} described by {describedBy}</command>
</commands>